import React from "react";
// import styled from "styled-components";
import Nav from "../components/Nav";

const HUSH = () => {
    return (
        <div>
            <Nav/>
            <p>HUSH</p>
        </div>
    );
};

export default HUSH;
